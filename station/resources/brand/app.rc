// 윈도우 실행 파일에 박히는 아이콘.
//
// 탐색기와 작업 표시줄이 .exe 자체에서 읽는 것이라, 실행 중에 거는
// setWindowIcon 으로는 대신할 수 없다. 프로그램을 켜기 전에도 아이콘이
// 보여야 조작자가 바탕화면에서 찾는다.
//
// IDI_ICON1(=1) 로 두는 것이 관례다. Qt 는 리소스 번호 1 번을 창 아이콘의
// 대비책으로도 쓴다.
IDI_ICON1 ICON "app.ico"
