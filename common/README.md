# 공통 계약

HMI와 로봇 소프트웨어가 함께 사용하는, 구현에 독립적인 계약을 둔다. `hmi/`와
`robot/`는 서로의 내부 헤더나 소스에 의존하지 않고 이 영역만 공유한다.

| 경로 | 역할 |
|---|---|
| `protocol/` | TCP 프레이밍 구현과 버전 있는 통신 규약 |

채널 이름, 메시지 구조, 오류 코드는 [통신 규격](../docs/bridge_protocol.md)을 단일
기준으로 삼는다.
