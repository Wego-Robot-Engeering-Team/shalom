#!/usr/bin/env bash
set -euo pipefail

root="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
export QT_PLUGIN_PATH="$root/plugins"
export LD_LIBRARY_PATH="$root/bin${LD_LIBRARY_PATH:+:$LD_LIBRARY_PATH}"
exec "$root/bin/inspection_hmi" "$@"
